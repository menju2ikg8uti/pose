{
  "name": "simple-nodejs",
  "version": "1.0.0",
  "description": "Simple Node.js repo",
  "main": "index.js",
  "scripts": {
     "build": "next build && echo 'Build complete'",
     "build": "nx next:build && echo 'Build complete'",
     "lint": "eslint ./src",
     "lint": "nx eslint:lint",
    "test": "node ./run-tests.js"
  },
   "nx": {}
}
